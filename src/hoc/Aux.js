// The Aux higher order component is used to combine a few children into
// one item. This is needed since components should only return one item.
const aux = (props) => props.children;

export default aux;